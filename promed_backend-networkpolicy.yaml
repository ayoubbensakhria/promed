apiVersion: extensions/v1beta1
kind: NetworkPolicy
metadata:
  creationTimestamp: null
  name: promed_backend
spec:
  ingress:
  - from:
    - podSelector:
        matchLabels:
          io.kompose.network/promed_backend: "true"
  podSelector:
    matchLabels:
      io.kompose.network/promed_backend: "true"
